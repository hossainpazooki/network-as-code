apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: egress-common
spec:
  podSelector: {}
  policyTypes:
    - Egress
  egress:
    - to:
        - ipBlock:
            cidr: 10.0.1.0/24
      ports:
        - protocol: TCP
          port: 5432
