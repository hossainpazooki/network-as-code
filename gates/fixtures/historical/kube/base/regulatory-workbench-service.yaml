apiVersion: v1
kind: Service
metadata:
  name: regulatory-workbench
  labels:
    app: regulatory-workbench
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: regulatory-workbench
